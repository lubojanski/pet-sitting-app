export class Sitter {
  constructor(
    public id: number,
    public name: string,
    public price: number,
    public city: string,
    public address: string,
    public description: string
  ) {  }
}